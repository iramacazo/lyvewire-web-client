import { StyleRules, Theme } from "@material-ui/core/styles";

export default (theme: Theme): StyleRules => ({
    box: {
        width: "100%",
    },
    root: {
        height: "100%",
        width: "100%",
    },
});
